import {
    useBlockProps,
    InspectorControls,
} from '@wordpress/block-editor';
import { PanelBody, RangeControl, SelectControl } from '@wordpress/components';

export function Edit( { attributes, setAttributes } ) {
    const { columns, category } = attributes;
    const blockProps = useBlockProps();

    return (
        <>
            <InspectorControls>
                <PanelBody title="Configuración del grid" initialOpen={ true }>
                    <RangeControl
                        label="Columnas"
                        value={ columns }
                        onChange={ ( value ) => setAttributes( { columns: value } ) }
                        min={ 1 }
                        max={ 6 }
                    />
                    <SelectControl
                        label="Categoría"
                        value={ category }
                        options={ [
                            { label: 'Todas', value: '' },
                            { label: 'Web', value: 'web' },
                            { label: 'Branding', value: 'branding' },
                        ] }
                        onChange={ ( value ) => setAttributes( { category: value } ) }
                    />
                </PanelBody>
            </InspectorControls>
            <div { ...blockProps }>
                <p>Portfolio Grid — { columns } columnas</p>
            </div>
        </>
    );
}
