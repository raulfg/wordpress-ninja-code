import { __ } from '@wordpress/i18n';
import { useBlockProps, InspectorControls } from '@wordpress/block-editor';
import { PanelBody, RangeControl, SelectControl } from '@wordpress/components';

export function Edit( { attributes, setAttributes } ) {
    const { columns, category } = attributes;
    const blockProps = useBlockProps( {
        style: { '--columns': columns },
    } );

    return (
        <>
            <InspectorControls>
                <PanelBody title={ __( 'Configuración', 'ninjatheme' ) }>
                    <RangeControl
                        label={ __( 'Columnas', 'ninjatheme' ) }
                        value={ columns }
                        onChange={ ( value ) => setAttributes( { columns: value } ) }
                        min={ 1 }
                        max={ 6 }
                    />
                    <SelectControl
                        label={ __( 'Categoría', 'ninjatheme' ) }
                        value={ category }
                        options={ [
                            { label: __( 'Todas', 'ninjatheme' ), value: '' },
                            { label: 'Web', value: 'web' },
                            { label: 'Branding', value: 'branding' },
                            { label: 'Fotografía', value: 'fotografia' },
                        ] }
                        onChange={ ( value ) => setAttributes( { category: value } ) }
                    />
                </PanelBody>
            </InspectorControls>
            <div { ...blockProps }>
                <p className="portfolio-grid__placeholder">
                    { __( 'Portfolio Grid — vista previa en el frontend', 'ninjatheme' ) }
                </p>
            </div>
        </>
    );
}

export function Save() {
    return null;
}
